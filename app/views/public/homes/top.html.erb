<div class="container">
  <div class="text-center">
    <p>ようこそ、ながのCAKEへ！<br>
    お客様のティータイムをすこしだけ豪華にする<br>
    手作り洋菓子をお届けいたします。</br>
  </div>

  <div class="text-center">
    <h1>新着商品</h1>
  </div>
    <div class="row">
       <div class="col-md-8 mx-auto d-flex justify-content-around">
          <% @new_products.each do |product| %>
          <div>
            <div>
              <%= link_to  customers_product_path(product) do %>
              <%= attachment_image_tag product, :image, :fill, 150, 150, fallback: "no_image.jpg", size: "150x150" %>
            </div>
            <% end %>
            <div class="text-center">¥<%= product.add_tax_price.to_s(:delimited) %> </div>
          </div>
         <% end %>
       </div>
    </div>
</div>