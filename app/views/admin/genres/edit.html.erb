<h2 class="head-line title_h2 text-center" >ジャンル編集</h2> 
<%= form_with model: [:admin,@genre], local: true do |f| %>
  <div class="col-xs-offset-1">
    <i class="fas fa-candy-cane">ジャンル名</i>
    <%= f.text_field :name  %>
    <%= f.submit "変更を保存", class: "btn btn-info" %>
  </div>
<% end %>
